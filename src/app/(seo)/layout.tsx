/**
 * SEO pages layout — shares the same root layout
 * but provides a route group for organizational clarity.
 */
export default function SeoLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
